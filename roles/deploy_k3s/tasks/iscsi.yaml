---
# install open-iscsi
- name: install open-iscsi
  ansible.builtin.apt:
    name: "{{ item }}"
    state: present
  loop:
    - open-iscsi

# start open-iscsi nad enable on boot
- name: start open-iscsi and enable on boot
  systemd:
    name: iscsid
    enabled: yes
    state: started
